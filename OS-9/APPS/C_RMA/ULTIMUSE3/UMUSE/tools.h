/* TOOLS.H  Toolbox and Brush-Rep locas *//* Split from wmenu.h in 95-96 *//* Rep (little indicator) stuff */#define REPWID      4#define REPHGT      TOOLHGT#define REPRB       (brushx+REPWID-1)#define REPTOP      (brushy+REPHGT-1)#define REPHALF     (brushy+REPHGT/2)/* Toolbox stuff */#define PALW        2         /* (was 3) width of a menu/palette item */#define PALWP       (8*PALW)#define PALW2P      (4*PALW)  /* 1/2 of PALW but in Pixels */#define NTOOLS      (20+1)    /* how many horiz slots, plus dividers */#define TOOLWID     ((NTOOLS+1)*PALW)#define TOOLEFT     ((COLS-TOOLWID-REPWID)/2)#define TOOLRGHT    (TOOLEFT+TOOLWID)   /* was -1 */   /* X-locs of toolbox compartments */#define NRPE        7          /* 7 Note/rest palette end */#define DMODS       (8+1)      /* 9 DurMod palette start w/ divider */ #define NRIO        (DMODS+4)  /* 13 Note/Rest in/out */#define TOOLBOXS    (NRIO+1)   /* 14 1st slot (from 0) of actual tools */#define TOOLBOT     SCROLTOP#define TOOLHGT     23#define TOOLTOP     (tooly+TOOLHGT-1) /* ToolBox floating submenu */#define TOOLHALF    (tooly + TOOLHGT/2)/* eof tools.h */