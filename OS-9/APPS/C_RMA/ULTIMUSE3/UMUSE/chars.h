/* CHARS.H  Coco3 version only!   Keyboard, not KeySense. *//* Severely restricted hardware compared to MM/1's PC-XT keypad;*  left and right arrows are same as BS (^H) and TAB (^I).*  May want user to use ^B and ^F instead.*/#define UPARROW     12   /* ^L formfeed! */#define DNARROW     10   /* ^J linefeed */#define LFTARROW     8   /* same as ^H destructive BS */#define RITARROW     9   /* same as ^I TAB */#define ENTER       Ctrl('m')#define BS          Ctrl('h')      /* == Coco LftArrow *//* eof chars.h */