/* VMEM.H  Virtual Memory items */extern direct EVENT     *curevp,       /* current event pointer */     *database,     /* Bottom of current page (1st byte's addr) */     *curptop;      /* highest event ptr possible on current page */extern direct Index     nevents,  /* total # of events */     curind;   /* current index */extern direct sexy     bwchan,   /* Buffer window I/O channel */     group,    /* buffer group, usually our PID */     curpage,  /* current page no. */     nmpages;  /* how many pages are in memory */EVENT     *i2p(), *plusplus(), *decdec();#define EVTPAGE     34#define EVTARSIZ    4096#define EventsIn    (curpage==EVTPAGE)  /* or in "macros.h" *//* Must also #include "titles.h" to use these */#define VTITLES     EVTARSIZ  /* Base of Titles[TOTLINES][TLENP1] */#define VTITLSIZ    (TOTLINES*TLENP1)/* eof VMem.h */