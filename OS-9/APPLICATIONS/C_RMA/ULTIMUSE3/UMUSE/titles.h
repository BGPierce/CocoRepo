/* ~Sw640 TITLES.H   Size info for Title/Banner/Info screen *//* Never change next two, or old versions can't read new scores */#define TLINES 4    /* how many lines */#define TLEN   51   /* how many chars per line, w/out NULL */#define TLENP1 52   /* and with the NULL *//* Don't change these either!  Allows 9 title lines */#define TXLINES     5#define TOTLINES    9    /* (TLINES+TXLINES) */#define Titles ((char *)evtarray + VTITLES)/* Diddle this at will */#define TWINTOPY    170  /* top edge of menu window *//* eof titles.h */